# Template of /home/ailab/ASR/Qwen3ASR/.env on localasr (loaded by wrapper_merged.py via WRAPPER_ENV_FILE).
# Secrets omitted — copy the real values from the box. Keys listed so a rebuild knows what wrapper_merged.py expects.
SUPABASE_URL=
SUPABASE_ANON_KEY=
FTP_HOST=
FTP_USER=
FTP_PASSWORD=
FTP_CDN_URL=
ELEVENLABS_API_KEY=
OPENAI_API_KEY=
TYPECAST_API_KEY=
GOOGLE_API_KEY=
TTS_SERVER_URL=
TTS_JWT_SECRET_KEY=
TTS_JWT_SECRET_KEY_LIVE=
TTS_JWT_SECRET_KEY_DEV=
ES_URL=
ES_USER=
ES_PASSWORD=

# No ANCHOR_* overrides in prod as of 2026-09-11 — all anchor tunables run on the defaults in anchor_service.py
# (the 2026-09-10 echo-floor 5->3 was moved from this file into the code default).
